@using JezekT.AspNetCore.IdentityServer4.WebApp.Views.Users
@using Microsoft.AspNetCore.Mvc.Localization
@using Microsoft.Extensions.Localization

@model JezekT.AspNetCore.IdentityServer4.WebApp.Models.UserViewModels.UserViewModel

@inject IViewLocalizer Localizer
@inject IStringLocalizerFactory StringLocalizerFactory
@{
    var sharedLocalizer = StringLocalizerFactory.Create(typeof(UserResources));
    ViewData["Title"] = Localizer["ViewTitle"];
}

<div class="page-header">
    <h1>@ViewData["Title"]</h1>    
</div>

<p>
    <a asp-action="Create" class="btn-bracketed">@sharedLocalizer["CreateNew"]</a>
</p>


<data-table url-data="@Url.Action("GetTableDataJsonAsync", "Users")" class="table table-striped table-bordered">
    <data-table-columns>
        <data-table-column asp-for="UserName"/>
        <data-table-column>
            <data-table-column-action action-title="@sharedLocalizer["Edit"]" action-url="@Url.Action("Edit", "Users")"/>
            <data-table-column-action action-title="@sharedLocalizer["Details"]" action-url="@Url.Action("Details", "Users")" />
            <data-table-column-action action-title="@sharedLocalizer["Delete"]" action-url="@Url.Action("Delete", "Users")" />
        </data-table-column>
    </data-table-columns>
</data-table>

